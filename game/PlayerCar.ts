class PlayerCar extends Car {
    private v: number;  // velocity
    private a: number;  // acceleration
    private d: number;  // drag
    private vm: number; // max velocity (from user input)
    private am: number; // max acceleration (from user input)
}